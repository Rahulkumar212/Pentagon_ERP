export interface CorporateFeed {
  category: string;
  title: string;
  text: string;
  author: string;
  date: string;
}

export interface CorporateFeedForm {
  title: string;
  text: string;
  category: string;
}